# Image tag to pull/run
VHM_IMAGE_TAG=edge-0.0.1

# Shared admin credentials used by built-in NPM and setup defaults.
# Use a strong password in your real .env file.
VHM_SHARED_EMAIL=admin@example.com
VHM_SHARED_PASSWORD=changeme

# Container timezone
TZ=Australia/Brisbane
